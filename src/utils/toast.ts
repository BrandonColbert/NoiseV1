//TODO: Create

/**
 * Displays a small popup with text at the bottom of the window
 * 
 * Allows feedback on actions to be provided
 */
export default class Toast {
	/**
	 * Removes this toast prematurely
	 */
	cancel(): void {

	}

	/**
	 * Display a new toast
	 * @param message 
	 * @param duration 
	 */
	static show(message: string, duration: number): Toast {
		return null
	}
}